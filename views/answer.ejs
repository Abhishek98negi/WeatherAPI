<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>city temp.</title>

        <!-- Bootstrap core CSS -->
        <!-- CSS only -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <style>
            body {background-color: powderblue;}

            p,h1,h2{
                color: #28747e;
            }
            button[type=submit] {
                height: 45px;
                width:70px;
              background-color: #2e8691;
              color: white;
              margin: 8px 0;
              border: none;
              cursor: pointer;
              border-radius: 4px;
                font-size: 100%;
            }
            button[type=submit]:hover {
              background-color: #28747e;
            }
        </style>

    </head>
    <body>
        <div class="jumbotron jumbotron-fluid">
          <div class="container">
            <h1 class="display-4"><%=nameOfCity%> : <%=citytemp%> &#xb0;c</h1>
            <p class="lead">The weather is currently <%=weather%></p>
            <form  action="/index" method="get">
                <button  type="submit" >Return</button>
            </form>

          </div>
        </div>

        <br><br>

        <div class="jumbotron jumbotron-fluid">
          <div class="container">
            <h2 class="display-4">History</h2>

            <% for (let i=posts.length-1; i>=0; i--){ %>
                <h2 class="lead"><%=posts[i].name %>: <%=posts[i].temp%>&#xb0;c &ensp; <%=posts[i].dateAndTime%></h2>
            <% } %>



          </div>
        </div>

    </body>
</html>
